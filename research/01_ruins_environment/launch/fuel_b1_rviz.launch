<launch>
  <!-- Reuse FUEL's own verified RViz launch. This wrapper adds no planner
       configuration and is deliberately kept separate from FUEL source. -->
  <include file="$(find exploration_manager)/launch/rviz.launch" />
</launch>
